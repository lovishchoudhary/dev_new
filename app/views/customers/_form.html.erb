<%= form_for @customer, :html => { :class => "form-horizontal customer" } do |f| %>

    <% if @customer.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @customer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :customer_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :customer_id, :class => 'form-control' %>
    </div>
    <%= error_span(@customer[:customer_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :customer_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :customer_name, :class => 'form-control' %>
    </div>
    <%= error_span(@customer[:customer_name]) %>
  </div>
  <div class="control-group">
    <%= f.label :customer_email, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :customer_email, :class => 'form-control' %>
    </div>
    <%= error_span(@customer[:customer_email]) %>
  </div>
  <div class="control-group">
    <%= f.label :customer_phone, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :customer_phone, :class => 'form-control' %>
    </div>
    <%= error_span(@customer[:customer_phone]) %>
  </div>
  <div class="control-group">
    <%= f.label :customer_location, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :customer_location, :class => 'form-control' %>
    </div>
    <%= error_span(@customer[:customer_location]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            customers_path, :class => 'btn btn-default' %>

<% end %>

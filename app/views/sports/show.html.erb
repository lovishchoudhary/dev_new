<%- model_class = Sport -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:sports_id) %>:</strong></dt>
  <dd><%= @sport.sports_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:sports_name) %>:</strong></dt>
  <dd><%= @sport.sports_name %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              sports_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_sport_path(@sport), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              sport_path(@sport),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
